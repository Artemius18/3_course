generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlserver"
   url     = "sqlserver://127.0.0.1:1433;database=JWT_lab17;user=Admin;password=123;encrypt=DANGER_PLAINTEXT"
}

model User {
  id        Int      @id @default(autoincrement())
  username  String   @unique
  password  String
}